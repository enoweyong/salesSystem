{
  "name": "novashop-backend",
  "version": "1.0.0",
  "private": true,
  "description": "NovaShop serverless backend — Lambda handlers (products CRUDL, orders, S3 presign)",
  "main": "src/products.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "seed": "node seed.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0"
  }
}
